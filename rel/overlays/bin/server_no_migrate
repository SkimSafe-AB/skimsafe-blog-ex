#!/bin/sh
set -eu

cd -P -- "$(dirname -- "$0")"

echo "Starting Phoenix server without running migrations..."
echo "Note: Run './migrate' separately before starting if migrations are needed"
exec ./skimsafe_blogg start